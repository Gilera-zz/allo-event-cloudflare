# V6.3 — Locked Hero

- Locks the desktop headline inside its left grid column so it cannot overlap the hero image.
- Preserves the approved editorial split and existing Allo Event header/logo.
- Uses a restrained photographic treatment for the fallback event-production image.
- Existing CMS-selected hero images/videos still override the fallback.
- No database migration required.
