# V6.2 — Approved Hero Match

This pass brings the live hero much closer to the approved visual reference.

- Preserves the existing Allo Event logo and `SiteHeader` unchanged.
- Reduces headline scale so the three lines stay entirely inside the left column.
- Widens the right media panel and increases media height.
- Makes the four proof cards taller and more editorial.
- Adds the approved dark backstage event visual as the **fallback only** when no real CMS hero image is configured.
- Any hero image selected in `Admin → Hemsida & hero` still replaces the fallback automatically.
- Keeps `WE MAKE IT HAPPEN.` as the right-side signoff.
- Keeps all existing V5/V6 admin, Case CMS, publishing safety, Massutskick and navigation functionality.
- No database migration required.
