# V6.1 — Editorial split hero

- Implements the approved split-layout hero reference.
- Headline: “FRÅN FÖRSTA INBÄRNING TILL SISTA UTLASTNING.”
- Keeps the clear staffing / production CTAs and concise service descriptor.
- Existing Homepage & Hero admin media/slideshow controls are reused inside the right-hand visual panel.
- No generated/demo event photography is added to the live site. When no real hero media is selected, the existing graphical fallback is used.
- Adds four compact proof points below the visual panel.
- Keeps the existing System / Light / Dark behavior and a light editorial fallback when no hero photo is configured.
- **The existing Allo Event logo asset and SiteHeader logo implementation are intentionally untouched.** The top-left logo is exactly the same asset used before V6.1.
- No database migration required.
