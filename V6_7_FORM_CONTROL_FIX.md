# V6.7 — Booking form browser consistency

- Replaced the public inquiry form's native HTML `<select>` with the existing Radix/Shadcn Select component.
- Dropdown menu now uses the Allo dark/light theme consistently in Edge, Chrome and Firefox instead of operating-system-native option styling.
- Normalized date input color-scheme and calendar indicator styling.
- No database migration required.
